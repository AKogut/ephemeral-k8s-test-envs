# Every path has an owner, so a review request is never ambiguous.
# On a single-maintainer repository this is one line — but the file is what makes
# that explicit rather than assumed, and it is the hook for adding reviewers per
# area later.

*                       @AKogut

# Areas where a change has consequences beyond the file being edited.
/charts/                @AKogut
/.github/workflows/     @AKogut
/scripts/               @AKogut
/docs/adr/              @AKogut
